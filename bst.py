class Binaryst:
    
    def __init__(self, value):
        self.value = value
        self.left = None
        self.right = None
    
    def insert(self, int) -> None:
    #- inserts a number into the tree
        
    
    
    
    def search(self, int) -> bool:
    #- returns whether or not a value is in the tree

    

    def in_order_traversal(self) -> list[int]#
    #- returns a list of all the values
      
    

    while current_node.left != None
        current_node = current.node.left
        
        return node.value
    
    

    def find_min(self) -> int:
    


    
    def find_max(self) -> int:
    #- returns the largest number in the tree\
    #(you cannot turn the tree into a list\
    #then return an element from the list, you must do it by traversing the tree)


    
    def height(self) -> int:
    #- returns the depth of the tree\
    #(how far is the furthest node from the root node?)


    
    def count_leaves(self) -> int:
    #- returns the number of leaf nodes\
    #in the tree (leaf nodes are nodes without any children)


    
    def serialize(self) -> str:
    #- turn the BST into a string


    
    def deserialize(self, tree: str) -> None:
    #- deserialize a serialized BST\
    #(take a string version of a BST\
    #and make an empty BST filled with\
    #those values). The new tree should\
    #match the tree that was serialized.


